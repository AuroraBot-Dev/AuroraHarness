# AuroraHarness 仓库根目录，也就是 uv / cargo 工作区所在的位置。
# 默认由模块位置推导（<repo>/src/frontend/modules -> 仓库根），
# 只有目录布局特殊时才需要显式设置。
# AURORA_ROOT=/absolute/path/to/AuroraHarness

# 浏览器联调用的 WebSocket 运行时地址。
# 只要设置了这一项，dev 模块就不再自动拉起本地运行时，改由你手动管理：
#   在仓库根运行 uv run aurora runtime --port 8765
# VITE_RUNTIME_WS=ws://127.0.0.1:8765/ws
